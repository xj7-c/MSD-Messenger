this project is ai generated and still got a lot of bugs 
If you want to run or build the project locally or package it into native iOS/Android binaries (via Capacitor or Cordova):
Extract the files on your computer and run:

Bash
<p>npm install
npm run dev</p>  
